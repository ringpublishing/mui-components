import { DateTimePickerProps } from '../../../../../../src/index.js';

const defaultArgs: Partial<DateTimePickerProps> = {
    className: '',
    sx: {},
    disabled: false,
};

export default defaultArgs;
