import React from 'react';
import { DateTimePicker } from '@ringpublishing/mui-components';

export default function DisabledExample(): React.JSX.Element {
    return <DateTimePicker disabled={true} />;
}
