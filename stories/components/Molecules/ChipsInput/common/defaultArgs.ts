import { ChipsInputProps } from '../../../../../src/index.js';

const defaultArgs: Partial<ChipsInputProps> = {
    labels: {
        title: 'Chips input',
        inputPlaceholder: 'Insert value and press enter',
        alreadyOnList: 'Already on list',
    },
};

export default defaultArgs;
