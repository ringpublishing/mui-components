import React from 'react';
import { Placeholder, PlaceholderVariant } from '@ringpublishing/mui-components';

export default function EmptyExample(): React.JSX.Element {
    return <Placeholder variant={PlaceholderVariant.EMPTY} />;
}
