import React from 'react';
import { Placeholder, PlaceholderVariant } from '@ringpublishing/mui-components';

export default function NotFoundExample(): React.JSX.Element {
    return <Placeholder variant={PlaceholderVariant.NOT_FOUND} />;
}
